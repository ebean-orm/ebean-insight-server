# Reference - https://ebean.io/docs/testing
ebean:
#  dumpMetricsOnShutdown: true
#  dumpMetricsOptions: loc,sql,hash
  test:
    shutdown: none # stop | remove
    platform: postgres # h2, postgres
    ddlMode: dropCreate # none | dropCreate |  migrations
    dbName: ebean_insight

datasource:
  db:
    username: ebean_insight
    password: test
    url: jdbc:postgresql://${docker.host}:6432/ebean_insight
